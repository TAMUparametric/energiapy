"""Parameter"""


class Parameter:
    """Input Parameter"""

    def __init__(self, 
                 )

    if not hasattr(self.model, self.name):
        
        setattr(self.model, self.name, kind(label=self.label))

        

    self.calculation = 

